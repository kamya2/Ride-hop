package com.alibou.security.mappers;

import com.fasterxml.jackson.databind.ObjectMapper;
import org.mapstruct.Mapper;

/**
 * TODO: Need to design Mapper inerface, done manually as of now (not efficient).
 */
@Mapper(componentModel = "spring")
public interface RideMapper {
//    Class<RideMapper> INSTANCE = RideMapper.class;
}
